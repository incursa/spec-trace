name: Validate

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Validate SpecTrace repository
        shell: pwsh
        run: ./scripts/Test-SpecTraceRepository.ps1 -Profile core
