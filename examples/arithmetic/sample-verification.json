{
  "artifact_id": "VER-MATH-DIV-0001",
  "artifact_type": "verification",
  "title": "Division operation verification",
  "domain": "arithmetic",
  "status": "passed",
  "owner": "platform-core",
  "related_artifacts": [
    "SPEC-MATH-DIV",
    "ARC-MATH-DIV-0001",
    "WI-MATH-DIV-0001"
  ],
  "scope": "Verify the narrow contract for the division operation.",
  "verifies": [
    "REQ-MATH-DIV-0001",
    "REQ-MATH-DIV-0002",
    "REQ-MATH-DIV-0003"
  ],
  "verification_method": "Automated execution of the operation with generated requirement-linked evidence.",
  "preconditions": [
    "the `arithmetic.divide` operation is available"
  ],
  "procedure": [
    "Invoke the operation without one operand and confirm the call is rejected.",
    "Invoke the operation with `numerator = 8` and `denominator = 2`.",
    "Confirm the result is `4`.",
    "Invoke the operation with `denominator = 0`.",
    "Confirm the call fails with a divide-by-zero error."
  ],
  "expected_result": "The operation requires both operands, returns the quotient for non-zero denominators, and rejects zero denominators.",
  "evidence": [
    "`division-evidence.evidence.json`"
  ],
  "status_summary": "This `passed` status applies to every requirement listed in `verifies`.\n\npassed"
}
