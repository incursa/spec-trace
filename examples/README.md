# Examples

This folder contains worked examples for the standard.

Canonical example artifacts are the JSON files in each example directory. Support docs and generated rollups in Markdown are explanatory only.

The examples are intentionally cross-linked across specifications, architecture or design, work items, verification artifacts, and generated evidence snapshots. They exist to show the model in use. They are not mandatory prose.

The generated `*.evidence.json` files in the examples are valid inputs for [`../scripts/Validate-SpecTraceEvidence.ps1`](../scripts/Validate-SpecTraceEvidence.ps1) and can be folded into the HTML and JSON report generated by [`../scripts/Render-SpecTraceAttestation.ps1`](../scripts/Render-SpecTraceAttestation.ps1).

The example set is also used as a validation target for [`scripts/Test-SpecTraceRepository.ps1`](../scripts/Test-SpecTraceRepository.ps1), including duplicate IDs, unresolved direct links, reciprocal trace consistency, namespace alignment, and profile checks.

## Inline Identifier References

Inline identifier references use backticks around stable IDs. They are lightweight prose links when a full `trace` block would be heavier than necessary.

Illustrative patterns:

```text
The button component MUST comply with `REQ-UI-CONVENTIONS-0001`.
Primary button styling MUST use `REQ-UI-TOKENS-0001`.
This specification builds on `SPEC-TPL`.
```

## Included Examples

### Payments

[`examples/payments/`](./payments/) is a product-style example built around ACH duplicate batch handling.

It demonstrates:

- a single specification file containing multiple related requirements
- compact requirement clauses
- traceability to architecture, work items, verification, and generated evidence
- optional upstream lineage and source references where the requirement history needs it
- inline identifier references in clauses, `notes`, and descriptive prose

### Arithmetic

[`examples/arithmetic/`](./arithmetic/) is a narrow technical example built around a division operation.

It demonstrates:

- a single narrow specification file
- method-level and edge-case requirements
- stable requirement IDs that can be referenced directly by tooling-produced evidence
- inline identifier references that keep shared rules visible without duplicating trace blocks
- verification artifacts with one shared status per artifact

### Integer Calculator

[`examples/calculator-int/`](./calculator-int/) is a compact but fuller technical example built around a signed 32-bit integer calculator.

It demonstrates:

- one specification covering a small but complete class-level contract
- precise `upstream_refs` into a local source note
- boundary handling for `Int32.MinValue` and `Int32.MaxValue`
- explicit overflow behavior instead of silent wraparound
- overlapping evidence snapshots for the same requirement set

### UI Design System

[`examples/ui-design-system/`](./ui-design-system/) is a compact design-system example built around shared conventions, tokens, and foundation rules for button and input components.

### BEM CSS

[`examples/bem-css/`](./bem-css/) is a BEM methodology example derived from the official BEM documentation.

## Entry Points

- [`../specs/requirements/spec-trace/`](../specs/requirements/spec-trace/) for the canonical SPEC suite
- [`payments/SPEC-PAY-ACH.json`](./payments/SPEC-PAY-ACH.json)
- [`payments/sample-architecture.json`](./payments/sample-architecture.json)
- [`payments/sample-work-item.json`](./payments/sample-work-item.json)
- [`payments/sample-verification.json`](./payments/sample-verification.json)
- [`payments/generated/duplicate-batch-evidence.evidence.json`](./payments/generated/duplicate-batch-evidence.evidence.json)
- [`arithmetic/SPEC-MATH-DIV.json`](./arithmetic/SPEC-MATH-DIV.json)
- [`arithmetic/sample-architecture.json`](./arithmetic/sample-architecture.json)
- [`arithmetic/sample-work-item.json`](./arithmetic/sample-work-item.json)
- [`arithmetic/sample-verification.json`](./arithmetic/sample-verification.json)
- [`arithmetic/generated/division-evidence.evidence.json`](./arithmetic/generated/division-evidence.evidence.json)
- [`calculator-int/source-notes.md`](./calculator-int/source-notes.md)
- [`calculator-int/SPEC-CALC-INT.json`](./calculator-int/SPEC-CALC-INT.json)
- [`calculator-int/sample-architecture.json`](./calculator-int/sample-architecture.json)
- [`calculator-int/sample-work-item.json`](./calculator-int/sample-work-item.json)
- [`calculator-int/sample-verification.json`](./calculator-int/sample-verification.json)
- [`ui-design-system/SPEC-UI-CONVENTIONS.json`](./ui-design-system/SPEC-UI-CONVENTIONS.json)
- [`ui-design-system/SPEC-UI-TOKENS.json`](./ui-design-system/SPEC-UI-TOKENS.json)
- [`ui-design-system/SPEC-UI-FOUNDATION.json`](./ui-design-system/SPEC-UI-FOUNDATION.json)
- [`ui-design-system/SPEC-UI-BUTTON.json`](./ui-design-system/SPEC-UI-BUTTON.json)
- [`ui-design-system/SPEC-UI-INPUT.json`](./ui-design-system/SPEC-UI-INPUT.json)
- [`bem-css/SPEC-BEM-CONCEPTS.json`](./bem-css/SPEC-BEM-CONCEPTS.json)
- [`bem-css/SPEC-BEM-NAMING.json`](./bem-css/SPEC-BEM-NAMING.json)
- [`bem-css/SPEC-BEM-CSS.json`](./bem-css/SPEC-BEM-CSS.json)
- [`bem-css/sample-architecture.json`](./bem-css/sample-architecture.json)
- [`bem-css/sample-verification.json`](./bem-css/sample-verification.json)
